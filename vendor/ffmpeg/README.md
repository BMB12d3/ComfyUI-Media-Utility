# FFmpeg runtime cache

This folder is populated on the user's machine by `setup_ffmpeg.py`.

Pinned runtime versions:
- `@ffmpeg/ffmpeg` 0.12.15
- `@ffmpeg/core` 0.12.10

The downloaded runtime files are intentionally excluded from the repository.
See `../../THIRD_PARTY_NOTICES.md`.
